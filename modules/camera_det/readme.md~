# 功能说明

相机数据收发demo

功能如下：

- [x] 接收多路相机数据、发送推理结果
- [x] 每一路数据在单独线程中推理，线程间不干扰
- [x] 低时延的数据接收、发送过程

# 运行说明

```shell
bash ./start.sh camera_det
```


#   1.0  版本是根据 planning 发过来的 model、ID_road_start、ID_road_last 确定加载轨迹。
        基本没什么问题，在持续性测试中
