
#   1.0  版本是根据 planning 发过来的 model、ID_road_start、ID_road_last 确定加载轨迹。
        基本没什么问题，在持续性测试中
        
#   2.0  1.0版本之上，完善了相机到激光雷达的投影参数，且将参数写入文档读写而不是代码中写死。
        现在备份，修改激光雷达传值到融合模块后延迟问题。还不确定是cm通信延迟还是代码处理逻辑延迟
        
#   3.0   修改了图像到点云的映射方式，修改了障碍物到中心轨迹点的距离求解方法，输出结果会卡在重复的 camera_data，备份后将解决这个问题。
                1126测试，目前看是解决了问题，算是比较稳定的一个版本，但障碍物距离轨道中心的距离判断上还存在问题，需要测试修改。
                
#   4.0   基本逻辑都算正常，启动时没有等到定位、没有加载轨迹点，会导致融合严重延迟。
                现在备份此程序，计算将融合模块单独起线程，在没有轨迹点信息前直接将激光雷达结果作为融合结果发送。
